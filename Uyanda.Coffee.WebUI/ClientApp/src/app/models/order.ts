export class Order {

    constructor(cartArray: [], cutomerID: any, redeemingPoints: boolean, currency: string) {
        
    }

    lineItems: any = [];

    Customer = {
        id: Number
    }

    IsRedeemingPoints: boolean = false

    Currency: string = ""
}